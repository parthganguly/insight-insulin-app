# R2 first patch implementation receipt

Date: 2026-09-16

**STATUS: R2 CATALOG IMPLEMENTED — INACTIVE — REVIEW PENDING**

## Base and authorization

Base and unchanged HEAD: `a322aad0ee9d0b1eb753aced18758de6eaff2935`.
Verified tree: `6ce4460d7ed0324be7def5825b9b5ac50ab68b80`.
Remote `refs/heads/main` was verified with `git ls-remote`; the fetched object
matches. Local `main` was stale and was not moved. New branch:
`codex/r2-reference-catalog`, separate `insight-r2-reference-catalog` worktree.
Changes are staged, uncommitted, ready for external review.

The owner's request incorporates `CODEX_R2_FIRST_PATCH.md` as this patch's scope.
The starter README/receipt were read and all eight `SHA256SUMS` entries verified
before input use. The historical September plan/ledger and earlier research
prompts are evidence, not authorization to execute old analyses or later R2-R5
work. All six AGENTS-required documents and the merged R1 report were read.
Older audits contain resolved findings and dated claims; they were not reapplied.
No unresolved contract conflict blocks this inactive patch. Future activation
must reconcile the new evidence/presentation contract separately.

Canonical J9 remains `f1db1b7798927bb75a5378f08543e23a4e34385a` on
`codex/j9-hardening-final`, with its two preexisting untracked J9 reports intact.
R1 remains `fb4394dc2c41d4dda571444d6cc2de605389bf8a` in its completed worktree;
the #134 worktree is also preserved. No reset, clean, stash or checkout was run
against these worktrees. Accepted research was read only.

## Actual implementation

Before editing, current name/alias/fuzzy lookup and decomposition/fallback calls
were inspected. Current production behavior remains on the ten-row legacy CSV;
R1 captures its loaded FNV identity and `current_backend_v2`. The new offline
compatibility/migration component has no runtime import or activation route.
Rust/UniFFI/native clients remain target components; native expansion stays
deferred under the July gate. Full contract and field dictionary:
[`docs/r2-reference-catalog.md`](../../docs/r2-reference-catalog.md).

| Added path | Purpose |
|---|---|
| `backend/reference_catalog.py` | Strict loader, immutable SHA-256 snapshot, use decisions, exact-ID/version selection and typed failures |
| `backend/build_reference_catalog.py` | Deterministic stdlib builder, input hashes and CSV/JSON equality, read-only regeneration check |
| `backend/reference_data/r2/inputs/*` | Byte-preserved authoritative corrected-metadata CSV/JSON |
| `backend/reference_data/r2/historical/*`, `INPUT_RECEIPT.json` | Literal issue/correction evidence and input lineage |
| `backend/reference_data/r2/eligibility_policy.json` | Separate initial per-use policy; no historical analysis masks |
| `backend/reference_data/r2/study_metadata.json` | Linked authenticated methods/population/sample context and access limitations |
| `backend/reference_data/r2/candidate_catalog.json` | Full 147-row inactive artifact, raw and mapped fields, embedded metadata/policy/issues |
| `backend/reference_data/r2/eligibility.csv` | 588 identity-bound per-record/per-use statuses and reasons |
| `backend/reference_data/r2/.gitattributes` | Preserve input/generated bytes across Git checkouts; original CSVs retain unusual line endings |
| `backend/tests/test_reference_catalog.py` | 17 focused contract/regression tests |
| `docs/r2-reference-catalog.md` | Build command, selection contract, policy, field dictionary and deferred integration seam |
| This report and `logs/*` | Implementation/review/check receipts; no owner data |

No existing tracked implementation/data file was changed. No new dependency,
framework, package-manager, global installation, browser/device operation or
provider call was needed.

## Input and artifact identity

| Input | SHA-256 |
|---|---|
| Corrected CSV | `2a8f9e612e493f7e93e363126f286807a72517cb994a6c90dc450c4d43aec4c9` |
| Corrected JSON | `3ebb953a3eec8510de588d06a03bfdef1bcdc5ec6915444ba36c8b5283ffa7a8` |
| Historical source ledger | `5d9f21b4826576cb2538ee54d1dac6e69f1bba6d461400c76c0801af1869271e` |

All 147 IDs, order and original values are unchanged. CSV/JSON semantic equality,
all raw JSON fields, and Git-index byte equality with the copied CSV/JSON/ledger
were checked. There is no second reference conversion, clipping, imputation,
carbohydrate repair, added Holt measurement or mixed-meal row. Synthetic tests
exercise zero FII; the actual supplied 147 records contain no zero FII mean.

Catalog: `r2_sha256_6db5357ae368981b1e2781a2a2402d60d63850e7b59c63e757b654a39bdc51d9`.
Schema: `insight_reference_catalog_v1`.
Eligibility: `experimental_fii_input_v1`.
Selection: `explicit_source_id_v1`.

Rebuild from root: `python backend/build_reference_catalog.py`.
Verify without writing: `python backend/build_reference_catalog.py --check`.
No environment secret or application startup is needed for these commands.

## Eligibility results and unresolved scientific issues

| Use | Candidate | Reference-only | Requires review |
|---|---:|---:|---:|
| Experimental FII input | 138 | 6 | 3 |
| Composition/energy context | 146 | 0 | 1 |
| GI/GL calculation | 134 | 11 | 2 |
| Fibre context | 144 | 3 | 0 |

The six Bell S1-001..006 rows remain FII reference-only because testing used
300 kJ, with source composition still per MJ. P02 carrot GL disagreement affects
GI/GL use, without correcting FII. P03 sausage keeps carbohydrate 35 and FII 7;
composition/energy and GI/GL uses require review, and its unresolved effect on
test-dose comparability also prevents FII-input selection. P06 lamb/noodle
text-table linkage/value conflicts prevent FII-input selection. All original
values remain available for source inspection. Missing GI/GL/fibre alone never
rejects an otherwise eligible FII source. All P01-P10 historical entries remain
literal; their old workflow statuses and statistical exclusions are not product
eligibility instructions.

Assumptions: the supplied corrected metadata is the authoritative numeric input;
existing source-transcription checks support candidate preparation, not a fresh
visual certification. Targeted reading of authenticated Bao/Bell methods supplies
study context, not guessed per-row sample sizes. Bao's group range 10-13 and Study
1 method n=10 are separate from unknown row counts; Bell's method n=10 does not
resolve 254 observations over 26 foods. Neither source's general method erases
the supplement's six explicit dose exceptions. No broader literature work was run.

Residual risks: historical source disputes remain unresolved; candidate status
does not validate a consumer-food analogy or arbitrary-portion prediction. The
hash identifies content, not publisher correctness or redistribution rights.
Future integration must persist server-owned selected-record snapshots and add
R3 unavailable-result/presentation semantics before any scoring activation. No
owner-data privacy surface is added; only public aggregate data and synthetic
test requests/databases were used.

## Tests actually run and independent review

Final checks used Python 3.13 with `PYTHON_DOTENV_DISABLED=1`; detailed outputs
are in `logs/`. Private interpreter/worktree prefixes are replaced by placeholders; trailing
whitespace is trimmed without removing warnings or test output.
Existing hardening tests print a deliberately synthetic secret-marker string;
no real secret was read. Existing FastAPI/Starlette/datetime deprecation warnings
and synthetic SQLite ResourceWarnings remain visible; no test was suppressed.

| Check (working directory: backend) | Final result |
|---|---|
| `python -m unittest tests.test_reference_catalog -v` | PASS, 17 |
| `python -m unittest discover -s tests -v` | PASS, 129 |
| `python -m unittest tests.test_meal_modeling_golden -v` | PASS, 8 frozen characterization cases |
| `python -m validation.run_validation` | PASS, 6/6 existing consistency cases, not clinical validation |
| `python -m validation.export_golden_fixtures --check --out ../crates/insight-core/fixtures/golden` | PASS; no fixture regeneration |
| `python -m compileall -q .` | PASS |
| `python build_reference_catalog.py --check` | PASS; artifact and reason output identical |
| `git diff --cached --check` (root) | PASS |

Tests cover source identity/mapping, every raw field, deterministic regeneration,
reduced dose, named disputes, optional missing fields, zero versus null, FII above
100, invalid/nonfinite/oversized numeric values, duplicate JSON keys/IDs, ambiguous
names, unsupported versions, overrides, deep immutability, snapshot lifetime,
policy-only identity changes, forged derived decisions, and unchanged runtime
identity/formula/import boundary. Existing backend coverage includes synthetic
R1 persistence/migration/replay paths. No new schema/migration is introduced.

No Python formatter or linter is configured in this repository/toolchain; no new
formatter/linter was installed. Syntax compilation, focused source review and
diff whitespace checks passed. Frontend/browser/device and Rust suites were not
rerun: no corresponding files, imports or numerical contracts changed, and the
existing backend golden export remains unchanged. No live backend was started.

The initial focused run had one incorrect test assumption that actual source rows
included a zero FII mean. It was removed after inspecting the data; explicit
synthetic zero/null/>100 tests remain. Independent reviewer `/root/r2_diff_review`
reviewed the staged actual diff and found two loader gaps: stripped DOI-only study
metadata was accepted, and oversized integers leaked `OverflowError`. Required
study/policy/issue shapes are now validated, and oversized values raise
`CatalogError`; regressions were added. The reviewer rechecked the correction and
field/policy documentation, independently reran all 17 catalog tests and build
`--check`, and reported no remaining findings. Final full checks above ran after
the corrections. Independent implementation review is complete; owner/data
acceptance and any later activation/release review remain pending.

## Review handoff and stop boundary

The single `INSIGHT_R2_CATALOG_REVIEW_20260916.zip` contains the exact changed files,
a binary-capable Git patch against the pinned base, input/task identifiers,
per-record eligibility, documentation, this receipt, retained check logs and a
SHA-256 manifest. No publisher PDF/DOCX, original historical archive ZIP, secret,
real meal or owner database is included. The patch is staged but uncommitted.

ACTIVE SCORING / API / DATASET / FORMULA CHANGES: NONE

OWNER DATA / OTHER WORKTREES / RESEARCH / PROVIDER MODIFICATIONS: NONE

COMMIT / PUSH / PR / MERGE / RELEASE: NONE

This completes only the inactive R2 foundation. It is not R2 integration or release.
